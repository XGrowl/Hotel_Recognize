<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.a80797.bookhotel_recognize">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".travel.travelDetailActivity"></activity>
        <activity android:name=".travel.TravelActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".facility.gym.GymActivity" />
        <activity android:name=".pay.failPayActivity" />
        <activity android:name=".pay.successPayActivity" />
        <activity android:name=".pay.hotelDetailBottomActivity" />
        <activity android:name=".mine.MineActivity"></activity>
        <activity android:name=".pay.PayActivity" />
        <activity android:name=".hotel.hotelDetailActivity" />
        <activity android:name=".WelcomeActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>